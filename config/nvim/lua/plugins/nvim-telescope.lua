require('telescope').setup {
  defaults = {},
  pickers = {
    buffers = {
      theme = "ivy"
    },
    find_files = {
      theme = "ivy"
    },
    git_files = {
      theme = "ivy"
    },
    live_grep = {
      theme = "ivy"
    }
  }
}

